/*
const getTemplate = (id, className) => {
  commentTemplate = commentsContainer.firstElementChild;
  //if (commentTemplate) { throw new Error('commentTemplate не найден!'); }

  return document.querySelector('#picture').content.querySelector('.picture');
};

const append = (dataElements, makeElement, container) => {
  dataElements.forEach((dataElement) => {

    //const element = commentTemplate.cloneNode(true);
    const element = makeElement(dataElement)

    //container.append(newCommentElement);
  });

  //container.append(...dataElements.map(makeElement));
};
*/
